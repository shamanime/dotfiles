# vi: ft=zsh

if [[ "$OSTYPE" == darwin* ]] && command -v ssh-add >/dev/null 2>&1; then
  # Reload any identities already saved in the macOS keychain.
  ssh-add --apple-load-keychain -q >/dev/null 2>&1
fi
