# vi: ft=zsh

export PATH="/opt/homebrew/bin:/opt/homebrew/sbin:$HOME/.local/bin:$PATH"

[ -x /opt/homebrew/bin/brew ] && eval "$(/opt/homebrew/bin/brew shellenv 2>/dev/null)"
command -v mise >/dev/null 2>&1 && eval "$(mise activate zsh)"
export PATH="$PATH:$HOME/.bin:$HOME/.local/bin:./bin:${COREUTILS_BIN}:${GPG_BIN}:${GNU_SED_BIN}:$FLYCTL_HOME/bin:${CARGO_BIN}:${OPENJDK_BIN}:/usr/local/bin:${POSTGRES_BIN}:${OPENCODE_BIN}:/usr/local/lib:/usr/local/sbin"
